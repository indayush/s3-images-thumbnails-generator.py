# s3-images-thumbnails-generator.py
Python code to generate thumbnails for images present in a S3 Bucket


Run the following to install the packages.
```
pip install -r requirements.txt
pip install --upgrade pillow
```
